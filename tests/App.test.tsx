// function setup(jsx: ReactNode) {
//   return {
//     user: userEvent.setup(),
//     ...render(jsx),
//   };
// }

describe("App", () => {
  test("input gets value", async () => {
    expect(true).toBe(true);
  });
});
